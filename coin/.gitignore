# Python
__pycache__/
*.pyc

# Generated model outputs — reproduce with the coin_*.py generators
# (e.g. `uv run coin_sandwich.py`). Never commit these.
*.stl
*.glb
*.png

# Slicer working files (Bambu Studio project/export). Force-add a real 3MF
# deliverable with `git add -f` if one is ever intended for the repo.
*.3mf
